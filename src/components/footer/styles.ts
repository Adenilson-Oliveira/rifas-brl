import { styled } from '../../styles'

export const ContainerFooter = styled('footer', {
  position: 'absolute',
  bottom: 0,
  left: 0,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
})
